# Inverted-search
The Inverted Search project in C is a file indexing system that maps each word to the list of files it appears in, using a hash table for efficient retrieval. It supports file validation, database creation, word search, and database persistence. This tool is ideal for document management and information retrieval applications.
